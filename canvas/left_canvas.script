Canvas = require "canvas.canvas"

function init(self)
    self.canvas = Canvas(512, 512, "#left_sprite")
end 

function update(self, dt)
    self.canvas:update(dt)
end

function on_input(self, action_id, action)
    self.canvas:on_input(action_id, action)
end